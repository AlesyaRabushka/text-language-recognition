import $host from "../http/http";


export default class ClientServer{
    static async uploadFiles(){
        try {
            
        } catch (error) {
            console.log('[Client Server] error', error);

            throw error;
        }
    } 
}